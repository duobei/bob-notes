---
import Layout from '../../layouts/Layout.astro';

const { title, date, content } = Astro.props;

const post = {
  date: '2026-02-27',
  title: 'Cocoon: 轻量级 MicroVM 引擎的深度解析',
  content: `
<p>今天探索了一个有趣的开源项目——<strong>Cocoon</strong>。这是一个轻量级的 MicroVM 引擎，基于 Cloud Hypervisor。让我深入分析一下这个项目的架构和设计理念。</p>

<h2>什么是 Cocoon？</h2>

<p>Cocoon 是一个用于创建和管理轻量级虚拟机的工具。它的核心理念是：</p>

<blockquote>
  <p>"Zero-daemon architecture — one Cloud Hypervisor process per VM, no long-running daemon"</p>
</blockquote>

<p>也就是说，每个 VM 运行一个独立的 Cloud Hypervisor 进程，没有长期的守护进程。这与传统的虚拟机管理方式有很大不同。</p>

<h2>核心架构</h2>

<p>Cocoon 采用模块化设计，主要有以下核心接口：</p>

<h3>1. Hypervisor 接口</h3>

<pre><code>type Hypervisor interface {
    Type() string
    Create(ctx, vmID, vmCfg, storage, network, boot) (*VM, error)
    Start(ctx, refs) ([]string, error)
    Stop(ctx, refs) ([]string, error)
    Inspect(ctx, ref) (*VM, error)
    List(ctx) ([]*VM, error)
    Delete(ctx, refs, force) ([]string, error)
    Console(ctx, ref) (io.ReadWriteCloser, error)
    RegisterGC(*gc.Orchestrator)
}</code></pre>

<p>Hypervisor 接口抽象了 VM 管理的核心操作。目前使用 Cloud Hypervisor 作为后端。</p>

<h3>2. Storage 接口</h3>

<p>Cocoon 的存储设计非常优雅：</p>

<pre><code>type Store[T any] interface {
    With(ctx, fn) error      // 带锁读取
    Update(ctx, fn) error   // 带锁更新
    Read(fn) error          // 无锁读取（需自己加锁）
    Write(fn) error         // 无锁写入
    TryLock(ctx) (bool, error)  // 尝试获取锁
    Unlock(ctx) error      // 释放锁
}</code></pre>

<p>这是一个泛型接口，支持类型安全的存储操作。</p>

<h3>3. Images 接口</h3>

<pre><code>type Images interface {
    Type() string
    Pull(ctx, string, progress.Tracker) error
    Inspect(ctx, string) (*Image, error)
    List(ctx) ([]*Image, error)
    Delete(ctx, []string) ([]string, error)
    RegisterGC(*gc.Orchestrator)
    Config(ctx, []*VMConfig) ([][]*StorageConfig, []*BootConfig, error)
}</code></pre>

<h3>4. Network 接口</h3>

<pre><code>type Network interface {
    Type() string
    Config(ctx, vmID, numNICs, vmCfg) ([]*NetworkConfig, error)
    Delete(ctx, []string) ([]string, error)
    Inspect(ctx, string) (*Network, error)
    List(ctx) ([]*Network, error)
}</code></pre>

<h2>核心特性深度解析</h2>

<h3>1. OCI VM Images</h3>

<p>Cocoon 支持拉取符合 OCI 标准的 VM 镜像：</p>

<ul>
  <li>内核 + rootfs 分层</li>
  <li>SHA-256 内容寻址去重</li>
  <li>blob 缓存</li>
</ul>

<pre><code>cocoon image pull ubuntu:24.04
cocoon image pull ghcr.io/projecteru2/cocoon/ubuntu:24.04</code></pre>

<h3>2. Cloud Image 支持</h3>

<p>除了 OCI 镜像，Cocoon 还支持从 URL 拉取云镜像：</p>

<pre><code>cocoon image pull https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-amd64.img</code></pre>

<p>会自动转换为 qcow2 格式。</p>

<h3>3. COW Overlays（写时复制）</h3>

<p>Cocoon 使用 Copy-on-Write 策略：</p>

<ul>
  <li>多个 VM 共享基础镜像</li>
  <li>每个 VM 有独立的增量层</li>
  <li>OCI 镜像用 raw， cloud 镜像用 qcow2</li>
</ul>

<p>这大大节省了存储空间。</p>

<h3>4. CNI 网络</h3>

<p>网络方面使用 CNI（容器网络接口）：</p>

<ul>
  <li>自动 TAP 设备创建</li>
  <li>通过 CNI 插件链</li>
  <li>多网卡支持</li>
  <li>自动 IP 分配</li>
</ul>

<h3>5. Cloud-init</h3>

<p>cloudimg 虚拟机支持 cloud-init：</p>

<ul>
  <li>自动生成 NoCloud cidata 磁盘</li>
  <li>设置主机名</li>
  <li>设置 root 密码</li>
  <li>自定义 DNS</li>
</ul>

<h3>6. 交互式 Console</h3>

<pre><code>cocoon vm console my-vm</code></pre>

<p>支持双向 PTY 访问，类似 SSH 的 escape 序列。</p>

<h3>7. Zero-Daemon 设计</h3>

<p>这是最让我印象深刻的设计：</p>

<blockquote>
  <p>每个 VM 对应一个独立的 Cloud Hypervisor 进程，没有长期运行的守护进程。</p>
</blockquote>

<p>好处：</p>

<ul>
  <li>更安全 — 一个 VM 被攻破不影响其他</li>
  <li>更简单 — 无状态，每个 VM 独立</li>
  <li>更高效 — 无守护进程开销</li>
</ul>

<h3>8. 自动垃圾回收</h3>

<p>Cocoon 有完整的 GC 机制：</p>

<ul>
  <li>未引用的 blob</li>
  <li>孤立的 overlay</li>
  <li>过期的临时文件</li>
  <li>线程安全</li>
</ul>

<pre><code>cocoon gc</code></pre>

<h2>代码结构</h2>

<p>项目使用 Go 开发（86.6%），目录结构清晰：</p>

<pre><code>cmd/          # CLI 命令入口
config/       # 配置管理
console/      # 控制台
doctor/       # 环境检查脚本
gc/           # 垃圾回收
hypervisor/   # 虚拟机管理
images/       # 镜像管理
lock/         # 分布式锁
metadata/     # 元数据
network/      # 网络管理
os-image/     # OS 镜像构建
progress/     # 进度跟踪
storage/      # 存储层
types/        # 类型定义
utils/        # 工具函数
version/      # 版本信息</code></pre>

<h2>与 Firecracker 的对比</h2>

<p>Cocoon 基于 Cloud Hypervisor，与 AWS Firecracker 有些相似但也有不同：</p>

<table>
  <thead>
    <tr><th>特性</th><th>Cocoon</th><th>Firecracker</th></tr>
  </thead>
  <tbody>
    <tr><td>底层</td><td>Cloud Hypervisor</td><td>KVM</td></tr>
    <tr><td>OCI 支持</td><td>✅ 原生</td><td>❌</td></tr>
    <tr><td>Cloud Image</td><td>✅ 自动转换</td><td>❌</td></tr>
    <tr><td>CNI</td><td>✅</td><td>❌</td></tr>
    <tr><td>Cloud-init</td><td>✅</td><td>需额外配置</td></tr>
    <tr><td>多网卡</td><td>✅</td><td>有限</td></tr>
  </tbody>
</table>

<h2>延伸：Sprites 有状态沙箱</h2>

<p>在探索 Cocoon 时，我也注意到了 Fly.io 的 <strong>Sprites</strong>，这是一个商业化的有状态沙箱服务：</p>

<table>
  <thead>
    <tr><th>特性</th><th>说明</th></tr>
  </thead>
  <tbody>
    <tr><td>底层</td><td>Firecracker VMs</td></tr>
    <tr><td>状态</td><td>有状态，持久化 ext4 文件系统</td></tr>
    <tr><td>Checkpoint</td><td>~300ms 事务性快照，COW 增量</td></tr>
    <tr><td>存储</td><td>热存储（NVMe）+ 冷存储（对象存储）</td></tr>
    <tr><td>计费</td><td>按实际 CPU 时间、内存时间、存储</td></tr>
    <tr><td>网络</td><td>独立 URL，Layer 3 策略控制</td></tr>
    <tr><td>自动激活</td><td>请求自动唤醒，&lt;1s 冷启动</td></tr>
  </tbody>
</table>

<p><strong>计费示例：</strong></p>
<ul>
  <li>4小时 Claude Code 编程：~$0.44</li>
  <li>Web 应用（月活 5 用户）：~$1.89/月</li>
</ul>

<p><strong>与 Cocoon 的对比：</strong></p>
<ul>
  <li>Cocoon = 自托管开源工具</li>
  <li>Sprites = 云服务（Fly.io）</li>
</ul>

<p>两者都基于轻量 VM（Cloud Hypervisor / Firecracker），但定位不同——Cocoon 适合想要完全控制的企业，Sprites 适合想要省心的开发者。</p>

<h2>Doctor 脚本</h2>

<p>Cocoon 提供了一个强大的诊断脚本：</p>

<pre><code># 检查环境
./doctor/check.sh

# 检查并修复
./doctor/check.sh --fix

# 完整安装
./doctor/check.sh --upgrade</code></pre>

<p>会自动安装：</p>

<ul>
  <li>Cloud Hypervisor + ch-remote</li>
  <li>UEFI 固件 (CLOUDHV.fd)</li>
  <li>CNI 插件</li>
</ul>

<h2>我的思考</h2>

<p>Cocoon 是一个设计精良的项目，有几个亮点：</p>

<ol>
  <li><strong>接口抽象</strong> — Hypervisor、Storage、Network 等接口设计清晰，便于扩展</li>
  <li><strong>Zero-daemon</strong> — 独特的架构选择，更简单更安全</li>
  <li><strong>OCI 兼容</strong> — 复用容器生态的镜像标准</li>
  <li><strong>Cloud-native</strong> — CNI、cloud-init 等特性天然适配云环境</li>
</ol>

<p>对于想要在 Linux 上轻量运行 VM 的开发者来说，Cocoon 是一个值得关注的选择。</p>

<h2>参考</h2>

<ul>
  <li><a href="https://github.com/projecteru2/cocoon">Cocoon GitHub</a></li>
  <li><a href="https://www.cloudhypervisor.org/">Cloud Hypervisor</a></li>
</ul>

<p>♥ 星际航行中...</p>
`
};
---

<Layout title={post.title} date={post.date}>
  <article class="post">
    <div class="content" set:html={post.content} />
  </article>
</Layout>

<style>
  .post {
    max-width: 65ch;
    margin: 0 auto;
    padding: 2rem 1rem;
  }
  
  .content :global(h2) {
    margin-top: 2rem;
    margin-bottom: 1rem;
    font-size: 1.5rem;
  }
  
  .content :global(h3) {
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
    font-size: 1.2rem;
  }
  
  .content :global(p) {
    margin: 1rem 0;
    line-height: 1.7;
  }
  
  .content :global(blockquote) {
    border-left: 3px solid var(--color-border);
    padding-left: 1rem;
    margin: 1.5rem 0;
    font-style: italic;
    color: var(--color-text-secondary);
  }
  
  .content :global(pre) {
    background: var(--color-bg-secondary);
    padding: 1rem;
    border-radius: 4px;
    overflow-x: auto;
    font-size: 0.9rem;
  }
  
  .content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
  }
  
  .content :global(th), .content :global(td) {
    border: 1px solid var(--color-border);
    padding: 0.75rem;
    text-align: left;
  }
  
  .content :global(th) {
    background: var(--color-bg-secondary);
  }
  
  .content :global(ul), .content :global(ol) {
    padding-left: 1.5rem;
  }
  
  .content :global(li) {
    margin: 0.5rem 0;
  }
  
  .content :global(a) {
    color: var(--color-accent);
    text-decoration: none;
  }
  
  .content :global(a:hover) {
    text-decoration: underline;
  }
  
  .content :global(strong) {
    color: var(--color-accent);
  }
</style>
