---
import Layout from '../../layouts/Layout.astro';

const { title, date, content } = Astro.props;

const post = {
  date: '2026-02-20',
  title: 'Bub Skills 技能系统详解',
  content: `
<p>Bub 的技能系统 (Skills) 是一个可扩展的能力框架，通过 SKILL.md 定义技能元数据，支持动态加载和组合。</p>

<h2>内置 Skills</h2>

<h3>1. bub - 子任务调度</h3>
<p>用于运行子任务和生成子 agent 实例。当需要委派任务、隔离执行或使用不同工具集时使用。</p>

<h3>2. telegram - Telegram 消息发送</h3>
<p>通过 Telegram Bot API 发送/编辑消息，支持回复、主动推送通知。需要 BUB_TELEGRAM_TOKEN 环境变量。</p>

<h3>3. discord - Discord 集成</h3>
<p>Discord 消息发送、频道管理、Webhook 处理。需要 discord.py 和相关配置。</p>

<h3>4. gh - GitHub CLI</h3>
<p>通过 gh 命令行工具操作 GitHub，支持仓库、Issue、PR、Release、Actions 等。</p>

<h3>5. skill-creator - 技能创建指南</h3>
<p>指导创建新技能的框架，包括 SKILL.md 模板和工作流。</p>

<h3>6. skill-installer - 技能安装器</h3>
<p>从 curated 列表或 GitHub 仓库安装技能到共享目录。</p>

<h2>第三方 Skills (.agents)</h2>

<p>Bob 项目还包含一些预置的第三方技能模板：</p>

<ul>
<li><strong>jupyter-notebook</strong>: Jupyter notebook 创建和管理，用于实验、探索、教程</li>
<li><strong>pdf</strong>: PDF 文件读取、创建、审查，使用 reportlab、pdfplumber、pypdf</li>
<li><strong>spreadsheet</strong>: 电子表格操作 (.xlsx, .csv, .tsv)，使用 openpyxl、pandas</li>
</ul>

<h2>技能加载机制</h2>

<p>技能通过 loader.py 中的 DiscoveryService 发现，加载 SKILL.md 中的元数据（name、description、location）。模型运行时可使用 \$skill_name 提示来展开技能定义。</p>

<h2>使用示例</h2>

<pre><code>,skills.list
,skills.describe name=telegram
</code></pre>
  `
};
---

<Layout title={post.title}>
  <article class="post">
    <header class="post-header">
      <span class="date">{post.date}</span>
      <h1>{post.title}</h1>
    </header>
    <div class="content" set:html={post.content} />
    <div class="back-link">
      <a href={import.meta.env.BASE_URL}>← 返回首页</a>
    </div>
  </article>
</Layout>

<style>
  .post-header {
    text-align: center;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--color-border);
  }
  .date {
    font-family: var(--font-hand);
    color: var(--color-muted);
    font-size: 1rem;
  }
  .post-header h1 {
    font-size: 2.5rem;
    margin-top: 0.5rem;
    font-weight: 600;
  }
  .content {
    font-size: 1.1rem;
    line-height: 2;
  }
  .content :global(p) {
    margin-bottom: 1.5rem;
  }
  .content :global(h2) {
    font-size: 1.5rem;
    margin: 2.5rem 0 1rem;
    color: var(--color-accent);
  }
  .content :global(h3) {
    font-size: 1.2rem;
    margin: 2rem 0 0.5rem;
    color: var(--color-secondary);
  }
  .content :global(ul) {
    margin: 1rem 0 1.5rem 1.5rem;
  }
  .content :global(li) {
    margin-bottom: 0.5rem;
  }
  .content :global(pre) {
    background: var(--color-surface);
    padding: 1rem;
    border-radius: 4px;
    overflow-x: auto;
  }
  .content :global(code) {
    font-family: var(--font-mono);
    background: var(--color-surface);
    padding: 0.2em 0.4em;
    border-radius: 4px;
  }
  .content :global(a) {
    color: var(--color-accent);
    text-decoration: underline;
  }
  .back-link {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--color-border);
  }
  .back-link a {
    color: var(--color-muted);
    text-decoration: none;
    transition: color 0.2s;
  }
  .back-link a:hover {
    color: var(--color-accent);
  }
</style>
